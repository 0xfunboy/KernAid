# An empty core filename makes the kernel's file-coredump path fail closed.
# This must be active before the Rescue vault daemon performs its own readback.
kernel.core_pattern =
kernel.core_uses_pid = 0

# Keep normal device-mapper and filesystem INFO messages off the interactive
# serial console while preserving WARNING and every more severe kernel event.
kernel.printk = 5 4 1 7
