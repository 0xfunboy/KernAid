[package]
name = "kernaid-linux-nsfs"
version.workspace = true
edition.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.189"
rustix = { version = "1.1.4", features = ["fs"] }

[lints.clippy]
unwrap_used = "deny"
panic = "deny"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
