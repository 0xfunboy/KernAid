[package]
name = "kernaid-core"
version.workspace = true
edition.workspace = true
[features]
default = []
fixture-repair-lab = ["kernaid-policy/fixture-repair-lab"]
rescue-fstab-production-candidate = ["kernaid-policy/rescue-fstab-production-candidate"]
rescue-crypttab-production-candidate = ["kernaid-policy/rescue-crypttab-production-candidate"]
[dependencies]
kernaid-evidence = { path = "../evidence" }
kernaid-policy = { path = "../policy" }
kernaid-protocol = { path = "../protocol" }
sha2 = "0.10"
[lints]
workspace = true
